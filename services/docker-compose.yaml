services:
  postfix:
    image: ghcr.io/lsflk/silver-smtp:main
    container_name: postfix
    ports:
      - "25:25"
      - "587:587"
    volumes:
      - ./opengovmail-config/postfix/main.cf:/etc/postfix/main.cf:ro
      - ./opengovmail-config/postfix/master.cf:/etc/postfix/master.cf:ro
      - ../conf/opengovmail.yaml:/etc/postfix/opengovmail.yaml
      - ./opengovmail-config/certbot/keys/etc:/etc/letsencrypt:ro
      - ./opengovmail-config/raven/data:/app/data:rw
      - postfix-spool:/var/spool/postfix
      - postfix-logs:/var/log
    networks:
      - mail-network
    depends_on:
      opendkim:
        condition: service_started
      rspamd:
        condition: service_started
      certbot:
        condition: service_started
      raven:
        condition: service_started

  raven:
    image: ghcr.io/lsflk/raven:latest
    container_name: raven
    ports:
      - "143:143"
      - "993:993"
      - "24:24"
      - "12345:12345"
      - "9100:9100"
    volumes:
      - ./opengovmail-config/raven/conf:/etc/raven:rw
      - ./opengovmail-config/raven/data:/app/data:rw
      - ./opengovmail-config/raven/certs:/certs:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - DB_FILE=/app/data/databases/shared.db
      - SOCKETMAP_HOST=0.0.0.0
      - SOCKETMAP_PORT=9100
      - THUNDER_HOST=thunder
      - THUNDER_PORT=8090
      - CACHE_TTL_SECONDS=300
      - TOKEN_REFRESH_SECONDS=3300
    networks:
      - mail-network
    depends_on:
      thunder-setup:
        condition: service_completed_successfully
      thunder:
        condition: service_started
    restart: unless-stopped
    user: "0:0"

  opendkim:
    image: ghcr.io/lsflk/silver-dkim:main
    container_name: opendkim
    volumes:
      - ../conf/opengovmail.yaml:/etc/opendkim/opengovmail.yaml
      - ./opengovmail-config/opendkim/opendkim.conf:/etc/opendkim/opendkim.conf:ro
      - ./opengovmail-config/opendkim/KeyTable:/etc/opendkim/KeyTable:ro
      - ./opengovmail-config/opendkim/SigningTable:/etc/opendkim/SigningTable:ro
      - ./opengovmail-config/opendkim/TrustedHosts:/etc/opendkim/TrustedHosts:ro
      - dkim-keys:/etc/dkimkeys
    networks:
      - mail-network
    cap_drop:
      - all
    security_opt:
      - no-new-privileges:true

  rspamd:
    image: rspamd/rspamd:latest
    container_name: rspamd
    hostname: rspamd
    expose:
      - "11332"
    ports:
      - "11334:11334"
    volumes:
      - rspamd_data:/var/lib/rspamd
      - rspamd_logs:/var/log/rspamd
      - ./opengovmail-config/rspamd/:/etc/rspamd/local.d:ro
    networks:
      - mail-network

  redis:
    image: redis:alpine
    container_name: redis
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - mail-network

  # Local recursive DNS resolver (recommended)
  # Note: you can use any unbound image; this is an example. Ensure it is configured for recursion
  unbound:
    image: mvance/unbound:latest
    container_name: unbound
    restart: unless-stopped
    networks:
      - mail-network
    # Expose only to the compose network by default; no published ports are required
    # You can mount custom configs if desired:
    # volumes:
    #   - ./unbound:/opt/unbound/etc/unbound

  clamav:
    image: clamav/clamav:latest
    container_name: clamav
    volumes:
      - clamav_db:/var/lib/clamav
      - clamav_logs:/var/log/clamav
    networks:
      - mail-network
    restart: always
    depends_on:
      - rspamd
    environment:
      - CLAMAV_NO_FRESHCLAMD=false
    deploy:
      resources:
        limits:
          memory: 2.8G
        reservations:
          memory: 1G

  clamav-exporter:
    build:
      context: ./opengovmail-config/clamav-exporter
      dockerfile: Dockerfile
    container_name: clamav-exporter
    volumes:
      - clamav_logs:/var/log/clamav:ro
    expose:
      - "9810"
    networks:
      - mail-network
    restart: unless-stopped
    depends_on:
      - clamav

  # Initialize database from the image
  thunder-db-init:
    image: ghcr.io/asgardeo/thunder:0.32.0
    container_name: thunder-db-init
    command: sh -c "cp -r /opt/thunder/repository/database/* /data/ && (cp -r /opt/thunder/consent/repository/database/* /consent-data/ 2>/dev/null || true)"
    volumes:
      - thunder-db:/data
      - consent-db:/consent-data
    restart: "no"

  # Run setup once with the shared database
  thunder-setup:
    image: ghcr.io/asgardeo/thunder:0.32.0
    container_name: thunder-setup
    command: ./setup.sh
    volumes:
      - thunder-db:/opt/thunder/repository/database
      - consent-db:/opt/thunder/consent/repository/database
      - ./opengovmail-config/thunder/deployment.yaml:/opt/thunder/repository/conf/deployment.yaml:ro
      - ./../scripts/thunder/01-default-resources.sh:/opt/thunder/bootstrap/01-default-resources.sh
      - ./../scripts/thunder/02-sample-resources.sh:/opt/thunder/bootstrap/02-sample-resources.sh
    environment:
      - THUNDER_ADMIN_USERNAME=${THUNDER_ADMIN_USERNAME:-admin}
      - THUNDER_ADMIN_PASSWORD=${THUNDER_ADMIN_PASSWORD:-admin}
    depends_on:
      thunder-db-init:
        condition: service_completed_successfully
    restart: "no"

  # Run Thunder server with the shared database
  thunder:
    image: ghcr.io/asgardeo/thunder:0.32.0
    container_name: thunder
    depends_on:
      thunder-setup:
        condition: service_completed_successfully
    ports:
      - "8090:8090"
    volumes:
      - thunder-db:/opt/thunder/repository/database
      - consent-db:/opt/thunder/consent/repository/database
      - ./opengovmail-config/thunder/certs/server.cert:/opt/thunder/repository/resources/security/server.cert:ro
      - ./opengovmail-config/thunder/certs/server.key:/opt/thunder/repository/resources/security/server.key:ro
      - ./opengovmail-config/thunder/deployment.yaml:/opt/thunder/repository/conf/deployment.yaml:ro
      - ./opengovmail-config/thunder/console-config.js:/opt/thunder/apps/console/config.js:ro
      - ./opengovmail-config/thunder/gate-config.js:/opt/thunder/apps/gate/config.js:ro
    networks:
      - mail-network
    restart: unless-stopped

  certbot:
    image: ghcr.io/lsflk/silver-certbot:main
    container_name: certbot
    volumes:
      - ./opengovmail-config/certbot/keys/etc:/etc/letsencrypt
      - ./opengovmail-config/certbot/keys/log:/var/log/letsencrypt
      - ./opengovmail-config/certbot/keys/lib:/var/lib/letsencrypt"
    ports:
      - "80:80"
      - "443:443"
    networks:
      - mail-network

  opengovmail-metadata:
    image: ghcr.io/lsflk/silver-metadata-service:main
    container_name: opengovmail-metadata
    ports:
      - "8888:8888"
    volumes:
      - clamav_db:/var/lib/clamav:ro
    environment:
      - PORT=8888
      - CLAMAV_DB_PATH=/var/lib/clamav
      - EXTERNAL_API_URL=${EXTERNAL_API_URL}
      - PUSH_INTERVAL_SECONDS=${PUSH_INTERVAL_SECONDS:-60}
      - ENABLE_PUSH_SERVICE=${ENABLE_PUSH_SERVICE:-true}
      - API_KEY=${API_KEY}
    networks:
      - mail-network
      - observability
    restart: unless-stopped
    depends_on:
      - clamav

  postfix-exporter:
    image: chatwork/kumina-postfix-exporter:latest
    container_name: postfix-exporter
    command:
      - --postfix.showq_path=/var/spool/postfix/public/showq
      - --postfix.logfile_path=/var/log/mail.log
    volumes:
      - postfix-logs:/var/log:ro
      - postfix-spool:/var/spool/postfix:ro
    expose:
      - "9154"
    networks:
      - mail-network
    restart: unless-stopped
    depends_on:
      - postfix

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    command:
      - "--path.rootfs=/host"
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    volumes:
      - /:/host:ro,rslave
    expose:
      - "9100"
    networks:
      - mail-network
    restart: unless-stopped
    pid: host

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor
    privileged: true
    devices:
      - /dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    expose:
      - "8080"
    networks:
      - mail-network
    restart: unless-stopped

  loki:
    image: grafana/loki:3.6.7
    container_name: loki
    command: -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ./opengovmail-config/loki/loki-config.yaml:/etc/loki/loki-config.yaml:ro
      - loki-data:/loki
    restart: unless-stopped
    networks:
      - observability
      - mail-network

  promtail:
    image: grafana/promtail:3.5.8
    container_name: promtail
    volumes:
      - ./opengovmail-config/promtail/promtail-config.yaml:/mnt/config/promtail-config.yaml
      - /var/log:/var/log:ro
      - postfix-logs:/mnt/logs/postfix:ro
      - rspamd_logs:/mnt/logs/rspamd:ro
      - clamav_logs:/mnt/logs/clamav:ro
    depends_on:
      - loki
    command: -config.file=/mnt/config/promtail-config.yaml
    networks:
      - mail-network
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    env_file: ".env"
    volumes:
      - ./opengovmail-config/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=${PROMETHEUS_RETENTION_DAYS:-30d}"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
      - "--web.enable-lifecycle"
    restart: unless-stopped
    networks:
      - observability
      - mail-network

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    env_file: ".env"
    ports:
      - 3000:3000
    volumes:
      - grafana_data:/var/lib/grafana
      - ./opengovmail-config/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./opengovmail-config/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - ./opengovmail-config/grafana/certs/grafana.crt:/etc/grafana/grafana.crt:ro
      - ./opengovmail-config/grafana/certs/grafana.key:/etc/grafana/grafana.key:ro
      - ./opengovmail-config/grafana/grafana.ini:/etc/grafana/grafana.ini:ro
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=https://${MAIL_SERVER_IP:-localhost}:3000
    restart: unless-stopped
    networks:
      - observability
    depends_on:
      - prometheus

volumes:
  postfix-spool:
  postfix-logs:
  dkim-keys:
  rspamd_data:
  rspamd_logs:
  clamav_db:
  clamav_logs:
  go-imap-data:
  loki-data:
  prometheus_data:
  grafana_data:
  redis-data:
  thunder-db:
  consent-db:

networks:
  mail-network:
  observability:
    driver: bridge
